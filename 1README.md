# AI Research Agent (Semantic Kernel)

Project ready to run Semantic Kernel agent with RAG and FAISS.

Installation:
```
git clone <repo>
cd ai-research-agent
pip install -r requirements.txt
cp .env.example .env
```
Run:
```
python main.py
```
